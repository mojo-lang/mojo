INCLUDE(NcHeaderDirectory)
INCLUDE(NcSourceDirectory)

NC_SOURCE_DIRECTORY(SRCS FULL_SRCS mojo/core)
SET(MOJO_CORE_SOURCES ${FULL_SRCS} PARENT_SCOPE)

NC_HEADER_DIRECTORY(FULL_HEADERS ../include/mojo/core)
SET(MOJO_CORE_HEADERS ${FULL_HEADERS} PARENT_SCOPE)
